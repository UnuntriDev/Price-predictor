# Root Hydra config. Experiment composition only; deployment env and
# secrets live in pydantic-settings (see ADR 0002).
defaults:
  - paths: default
  - data: otodom
  - model: xgboost
  - training: default
  - _self_

seed: 42
experiment_name: price-predictor
